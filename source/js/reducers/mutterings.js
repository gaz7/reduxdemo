
const mutteringsReducer = (state = [{
    mutterText: 'mutter, mutter, mutter'
}], action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default mutteringsReducer
